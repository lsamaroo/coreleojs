/** 
 * A class of Constants
 * @module constants 
 */
define(function(require) {
    'use strict';

    return {
        /** 
         * One second in milliseconds
         * @constant {number}
         */
        ONE_SECOND: 1000,


        /** 
         * One minute in milliseconds
         * @constant {number}
         */
        ONE_MINUTE: 60000
    };

});